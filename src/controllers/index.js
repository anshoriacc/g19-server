const authController = require('./auth');
const profileController = require('./profile');
const productController = require('./product');

module.exports = { authController, profileController, productController };
